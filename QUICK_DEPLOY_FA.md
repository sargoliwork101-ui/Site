# ⚡ راهنمای سریع اجرای سایت

## 🚀 آپلود روی هاست (فقط ۳ قدم، بدون نیاز به Node.js)

> روی هاست **هیچ دستوری لازم نیست** اجرا کنی. همه‌چیز از قبل Build شده و بک‌اند PHP هم داخل زیپ است.

۱. فایل **`site-upload.zip`** را از همین ریپو دانلود کن.
۲. در cPanel وارد **File Manager** شو و برو داخل پوشه **`public_html`** (اگر فایل قبلی هست پاکش کن).
۳. فایل زیپ را **Upload** کن و بعد **Extract** را بزن — تمام شد! ✅

حالا آدرس دامنه‌ات را باز کن؛ سایت باید کامل بالا بیاید.

> 💡 سایت هم در روت دامنه (`yourdomain.ir`) و هم در ساب‌فولدر (`yourdomain.ir/site`) بدون هیچ تغییری کار می‌کند.

## 🔐 اولین ورود و نصب (مهم!)

۱. روی **آیکون قفل در فوتر سایت** کلیک کن.
۲. اگر بالای فرم نشان **🔒 حالت امن** دیدی، یعنی بک‌اند PHP وصل است و **ویزارد نصب** باز می‌شود:
   - یک **رمز قوی** (حداقل ۸ کاراکتر) انتخاب کن،
   - **ایمیل بازیابی‌ات** را وارد کن و **کد تاییدی که به ایمیلت می‌آید** را بزن.
۳. بعد از ورود، حتماً از تب **امنیت و کلمه عبور** بخش **«سؤالات بازیابی محلی»** را پر کن (۲ سؤال + پاسخ) — ریست اضطراری فقط با جواب درست این‌ها باز می‌شود؛ بدون آن‌ها **قفل** است.
۴. اگر نشان **🖥️ حالت محلی** دیدی (هاست بدون PHP)، ورود با رمز محلی است و ارسال ایمیل کار نمی‌کند.

> ⚠️ کد تایید **فقط در ایمیل تو** موجود است و عمداً هیچ‌وقت روی صفحه سایت نمایش داده نمی‌شود. این یک ویژگی امنیتی است نه باگ!

## 💾 بک‌آپ‌گیری (تک‌مرکز، خودکار)

- تنها مرکز بک‌آپ: پنل ← تب **پشتیبان‌گیری و دیتابیس** (دکمه «پشتیبان‌گیری» بالای پنل فقط میان‌بر همین‌جاست).
- فایل بک‌آپ، **کل سایت** را پوشش می‌دهد: محتوا، کاربران، تنظیمات امنیتی و سؤالات بازیابی. آن را جای امن نگه دار.
- **بک‌آپ خودکار:** هر تغییری بدهی تایمر از اول شروع می‌شود؛ اگر تا پایان زمان تنظیمی (۱ تا ۳۰ روز، پیش‌فرض ۷ روز) تغییر جدیدی نیاید، فقط **یک** نسخه 🤖 در همان جدول نسخه‌ها ثبت می‌شود — نه بیشتر. فقط ۵ نسخه خودکار آخر نگه داشته می‌شود؛ نسخه‌های دستی تو پاک نمی‌شوند. تایمر با ساعت واقعی جلو می‌رود؛ اگر موعدش وقتی برسد که پنل بسته است، حدود یک دقیقه بعد از باز شدن پنل همان یک نسخه ثبت می‌شود.
- **بک‌آپ‌ها کجا هستند؟** نسخه‌ها در حافظه داخلی همان مرورگر/دستگاه‌اند (کلید `embedded_portfolio_snapshots_v2` — با F12 ← تب Application ← Local Storage می‌بینی‌شان)؛ فایل JSON هرجا که دانلودش می‌کنی (Downloads)؛ رمزها و SMTP روی هاست. ⚠️ پاک کردن دیتای مرورگر نسخه‌ها را پاک می‌کند — همیشه یک فایل تازه جای امن داشته باش.
- توجه: حافظه مرورگر (~۵MB) محدود است — میتر حافظه را در همین تب ببین؛ اگر پر شد، فایل را دانلود کن و نسخه‌های قدیمی را حذف کن.

## 🌐 بعد از آپلود: دامنه خودت را ثبت کن

۱. وارد پنل مدیریت شو → تب **استودیو سئو** → فیلد آدرس اصلی سایت را به `https://yourdomain.ir` تغییر بده.
۲. (اختیاری) اگر دامنه‌ات با `arashtaheri.dev` فرق دارد، در فایل‌های `sitemap.xml` و `robots.txt` جایگزینش کن.
۳. (توصیه‌شده) در فایل `api/config.php` ثابت `MAIL_FROM` را یک ایمیل روی دامنه خودت بگذار تا ایمیل‌ها کمتر اسپم شوند.
۴. 📬 **صندوق ارسال اعلان‌ها:** پنل ← تب **امنیت و کلمه عبور** ← کارت «صندوق ارسال اعلان‌ها» → هاست/نام‌کاربری از قبل پر شده؛ فقط **رمز ایمیل** را وارد کن، **ذخیره** و بعد **«ارسال ایمیل تست»** را بزن. اگر تست سبز شد، کدهای تایید و پیام‌های تماس از این به بعد با همین صندوق ارسال می‌شوند. (رمز فقط روی سرور می‌ماند و هیچ‌وقت داخل گیت نمی‌آید.)

## 🛠️ اگر خواستی سورس را تغییر بدهی (روی کامپیوتر خودت)

```bash
cd resume-app
npm install
npm run dev      # اجرا برای تست: http://localhost:5173
npm run build    # ساخت خروجی جدید (داخل پوشه dist)
```

بعد از `npm run build` محتویات پوشه `dist` را زیپ و مثل قدم‌های بالا آپلود کن.

## 🧯 عیب‌یابی

| مشکل | راه‌حل |
|------|--------|
| صفحه سفید بعد از آپلود | کش مرورگر را پاک کن (`Ctrl+Shift+R`)؛ مطمئن شو `index.html` مستقیم داخل `public_html` است نه داخل یک پوشه اضافه |
| خطای ۵۰۰ | از پشتیبانی هاست بخواه `mod_rewrite` و `AllowOverride All` فعال باشد (روی ۹۹٪ هاست‌های cPanel پیش‌فرض فعال است) |
| کد تایید (OTP) به ایمیلم نمی‌آید | ۱) پوشه **اسپم** را چک کن ۲) مطمئن شو پوشه `api/` آپلود شده و بالای فرم ورود **🔒 حالت امن** است ۳) از پشتیبانی هاست بپرس تابع `mail()` در PHP فعال است یا نه |
| ریست اضطراری کار نمی‌کند | عمداً قفل است تا وقتی که از داخل پنل (تب امنیت) **سؤالات بازیابی** را ثبت کرده باشی |
| فرم تماس ایمیل نمی‌فرستد | در حالت امن از خود هاست ارسال می‌شود؛ در حالت محلی از سرویس کمکی formsubmit (اولین بار یک ایمیل فعال‌سازی باید تایید شود) |
| عکس‌ها لود نمی‌شوند | عکس‌های نمونه از Unsplash می‌آیند و با اینترنت ایران گاهی کندند؛ از پنل مدیریت عکس‌های خودت را جایگزین کن |
