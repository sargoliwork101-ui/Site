# ⚡ راهنمای سریع اجرای سایت (نسخه درست‌شده)

## مشکل چه بود؟

فایل `website.zip` فقط **سورس خام** بود (کدهای React/JSX). مرورگر نمی‌تواند سورس را مستقیم اجرا کند، برای همین وقتی آن را روی هاست آپلود می‌کردی سایت بالا نمی‌آمد (صفحه سفید یا خطا).

علاوه بر آن سه مشکل دیگر هم داشت که درست شد:

| # | مشکل | وضعیت |
|---|------|-------|
| ۱ | سورس باید Build می‌شد و خروجی آماده‌ای وجود نداشت | ✅ خروجی آماده ساخته شد: `site-upload.zip` |
| ۲ | مسیر فایل‌ها مطلق بود (`/assets/...`) و در ساب‌فولدر/بعضی هاست‌ها لود نمی‌شد | ✅ نسبی شد (`./assets/...`) — همه‌جا کار می‌کند |
| ۳ | فونت وزیرمتن هیچ‌جا لود نمی‌شد و ظاهر سایت به‌هم می‌ریخت | ✅ فونت داخل خود سایت قرار گرفت (بدون نیاز به CDN یا اینترنت خارجی) |
| ۴ | فایل `.htaccess` در ساب‌فولدر خراب بود و تمدید SSL رایگان را هم می‌شکست | ✅ بازنویسی شد و با همه هاست‌ها سازگار است |

## 🚀 آپلود روی هاست (فقط ۳ قدم، بدون نیاز به Node.js)

> روی هاست **هیچ دستوری لازم نیست** اجرا کنی. همه‌چیز از قبل Build شده.

۱. فایل **`site-upload.zip`** را از همین ریپو دانلود کن.
۲. در cPanel وارد **File Manager** شو و برو داخل پوشه **`public_html`** (اگر فایل قبلی هست پاکش کن).
۳. فایل زیپ را **Upload** کن و بعد **Extract** را بزن — تمام شد! ✅

حالا آدرس دامنه‌ات را باز کن؛ سایت باید کامل بالا بیاید.

> 💡 سایت هم در روت دامنه (`yourdomain.ir`) و هم در ساب‌فولدر (`yourdomain.ir/site`) بدون هیچ تغییری کار می‌کند.

## 🔐 ورود به پنل مدیریت

- روی **آیکون قفل در فوتر سایت** کلیک کن (یا دکمه ورود به پنل مدیریت).
- نام کاربری: `admin` — رمز عبور: `admin`
- ⚠️ حتماً بعد از ورود، از تب **امنیت و رمز عبور** رمزش را عوض کن.

## 🌐 بعد از آپلود: دامنه خودت را ثبت کن

۱. وارد پنل مدیریت شو → تب **استودیو سئو** → فیلد آدرس اصلی سایت را به `https://yourdomain.ir` تغییر بده.
۲. (اختیاری) فایل‌های `sitemap.xml` و `robots.txt` را باز کن و `your-domain.ir` را با دامنه واقعی‌ات جایگزین کن.

## 🛠️ اگر خواستی سورس را تغییر بدهی (روی کامپیوتر خودت)

```bash
cd resume-app
npm install
npm run dev      # اجرا برای تست: http://localhost:5173
npm run build    # ساخت خروجی جدید (داخل پوشه dist)
```

بعد از `npm run build` محتویات پوشه `dist` را زیپ و مثل قدم‌های بالا آپلود کن.

## 🧯 عیب‌یابی

| مشکل | راه‌حل |
|------|--------|
| صفحه سفید بعد از آپلود | کش مرورگر را پاک کن (`Ctrl+Shift+R`)؛ مطمئن شو `index.html` مستقیم داخل `public_html` است نه داخل یک پوشه اضافه |
| خطای ۵۰۰ | از پشتیبانی هاست بخواه `mod_rewrite` و `AllowOverride All` فعال باشد (روی ۹۹٪ هاست‌های cPanel پیش‌فرض فعال است) |
| فرم تماس ایمیل نمی‌فرستد | فرم از سرویس رایگان formsubmit استفاده می‌کند؛ اولین بار یک ایمیل فعال‌سازی به ایمیل مدیر می‌آید که باید تاییدش کنی |
| عکس‌ها لود نمی‌شوند | عکس‌های نمونه از Unsplash می‌آیند و با اینترنت ایران گاهی کندند؛ از پنل مدیریت عکس‌های خودت را جایگزین کن |
